version: "25.0.2"
services:
  pyservices:
    build: /
    container_name: speedtest-service
    ports:
      - "3002:3002"
    volumes:
      - ./:/speedtest-service
      - /__pycache__
#     networks:
#       - docker-network
# networks:
#   docker-network:
#     external: true
